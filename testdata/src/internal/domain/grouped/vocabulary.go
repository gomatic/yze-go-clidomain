package grouped

// Argument is one positional CLI argument, the group's shared vocabulary its
// verb subpackages speak — a command group extends the root vocabulary for its
// own subcommands, exactly as the reference layout does.
type Argument = string
